import { RegisterForm } from "../components/auth/RegisterForm"
import { AuthLayout } from "../layouts/AuthLayout"

export default () => {
  return (
    <AuthLayout>
      <RegisterForm />
    </AuthLayout>
  )
}